{
  "name": "jaxchange-client",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "serve": "^14.2.4"
  },
  "scripts": {
    "start": "npx serve -s build -l 3001",
    "build": "echo 'Building client...' && mkdir -p build && cp public/* build/"
  }
}
